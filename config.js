/**
 * Configuration file for API keys and small tunables.
 * Edit values here to change API keys or sample sizes.
 * WARNING: Do NOT commit real secrets to a public repo.
 */

// Windy webcams API key. Optional — leave as placeholder to use sample cameras.
export const WINDY_API_KEY = 'YOUR_WINDY_API_KEY_HERE';

// How many satellites to sample from the TLE list (lower for faster local testing).
export const SAMPLE_SATS = 50;

// N2YO API key. The app currently used a demo key; replace with a real key for production.
export const N2YO_API_KEY = 'DEMO-API-KEY';
